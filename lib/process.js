'use strict'

/**
 *  @method process
 *
 *  Examines the addresses of the hub and sweeps value to inputs.
 *  Returns the trytes of the sweep transactions and a new state copy.
 *
 *  @param {Hub} hub
 *  @param {State} state
 *  @param {string} seed
 *
 *  @return {object}
 */
function process (hub, state, seed) {
  // WIP
}

module.exports = process
